# Backend Dockerfile
FROM python:3.11-slim

WORKDIR /app

# Copy server files
COPY server/ .

# Install dependencies
RUN pip install -e .

# Expose port
EXPOSE 8000

# Start command
CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8000"]
